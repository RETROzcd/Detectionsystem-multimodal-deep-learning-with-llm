IMAGE_CUT_PROMPT = {}

IMAGE_CUT_PROMPT['image_cut_v1'] = """
请对上传的玩具外包装图进行图像分割，识别并切分为6个面（通常为盒子的左侧面、右侧面、正面、背面、上面、下面）。依据图中显著的实线/虚线分割线与常见包装盒的展开图布局，确定每一面的位置。

要求：
1. 对每个面输出一个检测框，格式采用 YOLO 坐标格式：[x_center, y_center, width, height]，所有数值归一化（相对于图像宽高）。
2. 每个面重叠尽量少；
3. 需要包含所有前景物体(商标、 文字、 条形码、 二维码等)；
4. 返回一个合法有效的 JSON 对象，结构如下：
   {
     "reason": "解释你如何根据视觉结构判断出这6个面的位置",
     "boundingboxes": [
       [x_center, y_center, width, height],
       ...
     ]
   }
4. JSON 中不得包含注释、额外文字或解释，输出中只包含 JSON。
"""

IMAGE_CUT_PROMPT['image_cut_v2'] = """
你是一名资深的包装结构分析专家，专门处理玩具包装盒的展开图。请根据提供的包装盒展开图图像，对图像中的结构进行分析，并将图像划分为6～10个独立的面（通常为：正面、背面、左侧面、右侧面、上面、下面），每个面对应盒子的一个结构面。

任务要求如下：
1. 根据图中明显的折线（通常为长实线或长虚线）以及常见纸盒展开图的结构布局，准确识别出每个面的位置；
2. 为每个面生成一个检测框，格式为 YOLO 格式（归一化中心点和宽高）：
[x_center, y_center, width, height]，所有数值均需归一化（即相对于图像的宽度和高度，范围为 0~1）；
3. 每个检测框应完整覆盖该面的全部内容，包括商标、文字、图案、条形码、二维码等前景元素；
4. 所有检测框之间尽量减少重叠，确保每个面划分清晰、独立；
5. 返回格式为一个合法有效的 JSON 对象，结构如下：
{
     "reason": "解释你如何根据视觉结构判断出这6个面的位置",
     "boundingboxes": [
       [x_center, y_center, width, height],
       ...
     ]
}
6. 输出中仅包含 JSON 内容，不得包含任何额外文字或注释（例如 // 或 /* */ 等）；
"""

IMAGE_CUT_PROMPT['image_cut_v3'] = """
你是一名资深的图像版面分析专家，专门处理玩具外包装盒的展开图。请根据提供的外包装盒展开图图像，对图像中的结构进行分析。

# 工作流程与原则
1.  **定位外包装盒的边界**: 根据图中明显的折线（通常为长实线或长虚线）、 包装盒的主要的背景颜色(纯色， 无任何图案、文字)、以及常见纸盒展开图的结构布局，准确识别出外包装盒的边界。
2.  **结合外包装盒的边界检测每个面的位置**: 根据外包装盒的边界，检测每个面的位置。通常外包装盒的展开图会包含6个面，分别是正面、背面、左侧面、右侧面、上面、下面。每个面需要包含商标、文字、图案、条形码、二维码等前景元素。每个面的元素主方向通常一致。
3.  **检查是否可以每个区域进一步拆分**： 如果这个面有明显的分界，则可以进一步拆分。
4.  **精调位置**： 对上一步检测到的每个面，根据前景元素的分布，精调位置， 尽量少的覆盖背景，尽量多的覆盖前景。

# 输出要求
1. 为每个面生成一个检测框，格式为 YOLO 格式（归一化中心点和宽高）：
[x_center, y_center, width, height]，所有数值均需归一化（即相对于图像的宽度和高度，范围为 0~1）；
2. 所有检测框之间尽量减少重叠，确保每个面划分清晰、独立；
3. 每个框尽量少的覆盖背景，尽量多的覆盖前景；
4. 所有框能覆盖所有文字、条形码、二维码等前景元素；
5. 返回格式为一个合法有效的 JSON 对象，结构如下：
{
     "reason": "解释你如何根据视觉结构判断出这6个面的位置",
     "boundingboxes": [
       [x_center, y_center, width, height],
       ...
     ]
}
5. 输出中仅包含 JSON 内容，不得包含任何额外文字或注释（例如 // 或 /* */ 等）；
"""